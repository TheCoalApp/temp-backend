import * as $runtime from "@prisma/client/runtime/library"

/**
 * @param vector
 */
export const getSimilarNotes: (vector: string) => $runtime.TypedSql<getSimilarNotes.Parameters, getSimilarNotes.Result>

export namespace getSimilarNotes {
  export type Parameters = [vector: string]
  export type Result = {
    id: string
    noteTitle: string
    noteDescription: string
    syncStatus: string
    createdAt: Date
    updatedAt: Date
    lockedBy: string | null
    tag: string | null
    userId: string
    podcastPath: string | null
  }
}
