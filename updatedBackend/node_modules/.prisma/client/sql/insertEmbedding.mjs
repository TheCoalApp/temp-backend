import { makeTypedQueryFactory as $mkFactory } from "@prisma/client/runtime/library"
export const insertEmbedding = /*#__PURE__*/ $mkFactory("INSERT INTO \"Embedding\" (id, \"noteId\", embedding, \"createdAt\")\nVALUES ($1, $2, $3::vector, NOW());")
