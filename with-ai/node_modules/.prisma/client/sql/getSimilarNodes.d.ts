import * as $runtime from "@prisma/client/runtime/library"

/**
 * @param vector
 */
export const getSimilarNodes: (vector: string) => $runtime.TypedSql<getSimilarNodes.Parameters, getSimilarNodes.Result>

export namespace getSimilarNodes {
  export type Parameters = [vector: string]
  export type Result = {
    noteid: string
    version: number
    noteTitle: string
    noteDescription: string
    syncStatus: string
    createdAt: Date
    updatedAt: Date
    tags: string[] | null
  }
}
